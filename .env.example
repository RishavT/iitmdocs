# Weaviate Cloud Configuration
WEAVIATE_URL=https://your-cluster.weaviate.cloud
WEAVIATE_API_KEY=your_weaviate_api_key

# OpenAI API Configuration (required - can be OpenAI key or AI Pipe token)
OPENAI_API_KEY=your_openai_api_key_or_aipipe_token

# Cohere API Configuration (required only if using Cohere embeddings)
COHERE_API_KEY=your_cohere_api_key

# Chat API Configuration (optional - defaults to OpenAI)
# Uncomment and set these to use AI Pipe or another OpenAI-compatible API
# CHAT_API_ENDPOINT=https://aipipe.org/openrouter/v1/chat/completions
# CHAT_MODEL=gpt-4o-mini

# Embedding Configuration (optional - defaults to openai)
# Uncomment and set these to use Cohere embeddings
# EMBEDDING_PROVIDER=cohere
# EMBEDDING_MODEL=embed-multilingual-v3.0
