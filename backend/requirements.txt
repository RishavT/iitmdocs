# Django backend for the IITM BS chatbot (replaces worker.js + pg/faq_api FastAPI).
Django>=5.1,<5.2
djangorestframework>=3.15,<3.16
django-cors-headers>=4.4
uvicorn>=0.30,<0.31
httpx>=0.27,<0.28
python-dotenv>=1.0.0

# Django's async FAQ reader reuses the model and pgvector query from pg/faq_api;
# the synchronous bootstrap path remains unchanged and uses the same DB stack.
SQLAlchemy>=2.0.0
psycopg[binary]>=3.2.0
pgvector>=0.3.0
